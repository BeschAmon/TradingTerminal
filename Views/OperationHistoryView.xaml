<UserControl x:Class="TradingTerminal.Views.OperationHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:TradingTerminal.ViewModels"
             d:DataContext="{d:DesignInstance vm:OperationHistoryViewModel}"
             mc:Ignorable="d"
             Background="Transparent">

    <Border CornerRadius="8" Background="White" BorderBrush="#E0E0E0" BorderThickness="1">
        <Grid>


            <!-- Список операций -->
            <ScrollViewer Margin="0,40,0,0">
                <ItemsControl ItemsSource="{Binding Operations}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,5">
                                <!-- Дата -->
                                <TextBlock Text="{Binding FormattedDate}" 
                                           Foreground="#666" 
                                           Margin="15,5,0,0"
                                           FontSize="12"/>

                                <!-- Карточка операции -->
                                <Border Margin="10,25,10,5" 
                                        Background="White" 
                                        CornerRadius="6"
                                        BorderThickness="1" 
                                        BorderBrush="#EEE"
                                        Padding="10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Иконка -->
                                        <Border Grid.Column="0" 
                                                Width="40" 
                                                Height="40" 
                                                CornerRadius="20"
                                                Background="{Binding IconColor}">
                                            <TextBlock Text="{Binding IconCode}" 
                                                       FontSize="20"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                                        </Border>

                                        <!-- Описание -->
                                        <StackPanel Grid.Column="1" Margin="10,0">
                                            <TextBlock Text="{Binding Title}" 
                                                       FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding Description}" 
                                                       Foreground="#666" 
                                                       FontSize="12"/>
                                        </StackPanel>

                                        <!-- Сумма -->
                                        <TextBlock Grid.Column="2" 
                                                   Text="{Binding FormattedAmount}"
                                                   Foreground="{Binding IsNegative, 
                                                       Converter={StaticResource BoolToColorConverter}}"
                                                   FontWeight="SemiBold"/>
                                    </Grid>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>